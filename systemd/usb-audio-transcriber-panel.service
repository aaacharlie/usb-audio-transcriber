[Unit]
Description=USB Audio Transcriber control panel (local web app)

[Service]
Type=simple
UMask=0077
ExecStart=@PANEL_COMMAND@ serve
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
